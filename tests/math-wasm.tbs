//import * as Math from "Math"; TODO: Standardize import

export function test(): float64 {
    let a:float64 = 0.1;
    let b:float64 = 0.1;
    let c = Math.sin(b + a);
    return c;
}

export function testSin(a:float64): float64 {
    return Math.sin(a);
}